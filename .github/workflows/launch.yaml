name: Hello world workflow

on:
  push:
    branches:
      - main:
  pull_request:
    branches:
      - main
  workflow_dispatch:

jobs:
  hello:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v2
      - name: hello world
        run: Write-Host "Hello World"
        shell: powershell
